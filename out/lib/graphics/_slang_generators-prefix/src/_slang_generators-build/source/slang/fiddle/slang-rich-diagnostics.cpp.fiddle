// GENERATED CODE; DO NOT EDIT
//
// input file: slang-rich-diagnostics.cpp

// BEGIN FIDDLE TEMPLATE OUTPUT:
#ifdef FIDDLE_GENERATED_OUTPUT_ID

#if FIDDLE_GENERATED_OUTPUT_ID == 0
GenericDiagnostic FunctionReturnTypeMismatchParams::toGenericDiagnostic() const
{
    GenericDiagnostic result;
    result.code = 30007;
    result.severity = "error";
    
    // Build message by interpolating parameters using StringBuilder
    StringBuilder messageBuilder;
    messageBuilder << "expression type '";
    if (expression_type)
    {
        messageBuilder << expression_type->toString();
    }
    else
    {
        messageBuilder << "<unknown type>";
    }
    messageBuilder << "' does not match function's return type '";
    if (return_type)
    {
        messageBuilder << return_type->toString();
    }
    else
    {
        messageBuilder << "<unknown type>";
    }
    messageBuilder << "'";
    result.message = messageBuilder.produceString();
    
    // Set primary span
    result.primarySpan.location = expression_location;
    result.primarySpan.message = "expression type";
    
    // Set secondary spans
    {
        DiagnosticSpan span;
        span.location = function_location;
        span.message = "function return type";
        result.secondarySpans.add(span);
    }
    
    return result;
}


#endif

#undef FIDDLE_GENERATED_OUTPUT_ID
#endif
// END FIDDLE TEMPLATE OUTPUT

// END OF FIDDLE-GENERATED FILE
